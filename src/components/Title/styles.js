import { StyleSheet } from "react-native";
const styles = StyleSheet.create({
    title: {
        fontSize: 40,
        marginBottom: 20,
        fontWeight: 'bold',
        color: 'pink',
    }
    },
)
export default styles
